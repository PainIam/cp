#include <bits/stdc++.h>
using namespace std;

int main(){
    int n;
    vector<int> p;
    cin >> n;
    while (n--){
        int x;
        cin>>x;
        vector.push_back(x);
    }

    return 0;
}
